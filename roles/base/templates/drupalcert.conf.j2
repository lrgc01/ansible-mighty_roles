# Self signed certificates generated by the ssl-cert package
# Don't use them in a production server!

ssl_certificate {{ cert_hash_list[0].server_cert_path }};
ssl_certificate_key {{ cert_hash_list[0].server_certkey_path }};
